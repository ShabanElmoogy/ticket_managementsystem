{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Fullstack Chat App (Render ready)",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install:all && npm run build --prefix backend && npm run build --prefix frontend && cd backend && npx prisma migrate deploy && npx prisma db seed",
    "start": "npm run start --prefix backend",
    "seed": "npm run db:seed --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
