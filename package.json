{
  "name": "chat-app",
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "description": "Fullstack Chat App",
  "scripts": {
    "install:all": "npm install --prefix api && npm install --prefix web",

    "build": "npm run install:all && npm run build --prefix web",

    "start": "npm run start --prefix api",

    "seed": "cd api && node drizzle/seed.js",

    "migrate": "cd api && npx drizzle-kit push:mysql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}