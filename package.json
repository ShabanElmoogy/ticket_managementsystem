{
  "name": "ticket-management-system",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building frontend...' && cd frontend && npm install && npm run build && echo 'Setting up backend...' && cd ../backend && npm install && npx prisma generate && echo 'Build complete!'",
    "start": "cd backend && npx prisma migrate deploy && node index.js",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev"
  },
  "keywords": ["portfolio", "react", "express", "prisma"],
  "author": "",
  "license": "ISC"
}