/** Shared primitive types with no dependencies on other domain files. */

export type UserRole = 'SUPER_ADMIN' | 'TENANT_ADMIN' | 'EMPLOYEE' | 'PROGRAMMER';

export type Priority = 'LOW' | 'MEDIUM' | 'HIGH' | 'URGENT';
